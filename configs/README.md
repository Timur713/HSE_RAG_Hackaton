# Configs

Current experiment specs live in `legal_hse.experiments.default_experiments()` so they can be imported directly from Colab and scripts.

Put larger JSON/YAML sweeps here when experiment grids become too big for the Python registry.
